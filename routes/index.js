const express = require('express');
const router = express.Router();

/* GET home page. */
//router.get('/books/*.html.gz', function(req, res, next) {
	//console.log("gzip");
	//res.set('Content-Encoding', 'gzip');
//});

module.exports = router;
