document.addEventListener('DOMContentLoaded', function(event) {
	
	var main = document.querySelector('#main');
	console.log(main);
	var books = main.querySelectorAll('#books a');
	console.log(books);
	//books.forEach (book => {
		//book.addEventListener('click', function(event) {
			
		//});
	//});
	
	
})
